<div class="add_comment" class="lead">
  <h4>Add a comment!</h4>
  <%= form_for [@post, @comment] do |f| %>
    <%= f.text_area :text, {class: "form-control", rows: "3"} %>
    <%= f.submit "Submit Comment", {class: "btn btn-primary"} %>
  <% end %>
</div>

